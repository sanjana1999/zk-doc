export function encrypt({
	publicKey,
	privateKey,
	fileDetails,
	patientName,
	age,
	prescriptions,
	diagnosis,
}) {
	console.log(
		publicKey,
		privateKey,
		fileDetails,
		patientName,
		age,
		prescriptions,
		diagnosis
	);
}
